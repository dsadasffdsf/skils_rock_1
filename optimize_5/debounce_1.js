//Знал про его существование, но не знал как пишется. Так что это задание я не сделал,но зато теперь знаю как пишется

// function debounce(func, delay) {
//   let timeout;
//   return () => {
//     clearTimeout(timeout);
//     timeout = setTimeout(func, delay);
//   };
// }
// const debouncedFunction = debounce(() => {
//   console.log('Вызвана функция с задержкой');
// }, 2000);

// debouncedFunction();
// debouncedFunction();
